#include <iostream>

using namespace std ;
 
int main()   
{  

    // Declares variables and their types
    int a, b ;
    double z ;
    char ch ;

    // Uses user defined input to assign values to the variables
    cin >> a >> ch >> z ;
    cout << "a = " << a << endl << "ch = " << ch << endl << "z = " << z << endl ;

    // Fix variable b to make the source compilable

    b = 8 ;
    cout << "b = " << b << endl ;

    return 0 ;
}