// Initializing Pointer Variables Using nullptr

int main()
{
    /*
    C++11 Standard provides the null pointer, nullptr, to initialize pointer variables.
    A pointer with the value nullptr points to nothing, and is called the null pointer.
    nullptr has a special value type that can be converted to any pointer type. The
    following statement declares p to be a pointer of type int and it also initializes it 
    to the null pointer:
    */
    int *p = nullptr ;
    /*
    Because the compiler that we have used to test the code has implemented this feature
    of C++11 Standard, we can initialize the pointer variable using the int value 0, using
    another pointer variable of the same type, or using nullptr. In C++, nullptr is a
    reserved word.
    */
}