// Arrays
#include <iostream>

using namespace std ;

int main()
{
    // The general form for declaring a one-dimensional array is: 
    // dataType arrayName[intExp] ;

    // This statement declares an array 'num' of five components. 
    // Each component is of type int. The component names are 
    // num[0], num[1], num[2], num[3], and num[4].
    int num[5] ;
}